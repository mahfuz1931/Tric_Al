                                                 // One
                                                 
              cout << max({3 * (abs(a[0] - a[1])), 3 * (abs(a[2] - a[1])), 3 * a[0], 3 * a[2], a[0] + a[1] + a[2]});
                                      
                                      
                                                 //Two
                                           auto it=two.begin();
                                           auto x=*it;
                                           auto iit=three.lower_bound({x.ss,x.ff});
